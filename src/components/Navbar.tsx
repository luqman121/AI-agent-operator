"use client";
import { Menu, X } from "lucide-react";
import Image from "next/image";
import Link from "next/link";
import { useState } from "react";
import WhatsAppButton from "./WhatsAppButton";
const links = [{ label: "How It Works", href: "/#how-it-works" }, { label: "Features", href: "/#features" }, { label: "Industries", href: "/#industries" }, { label: "FAQ", href: "/#faq" }, { label: "Try It Free", href: "/demo" }];
export default function Navbar() { const [open, setOpen] = useState(false); return <nav className="fixed top-3 left-3 right-3 md:top-4 z-50 mx-auto max-w-4xl"><div className="backdrop-blur-xl bg-white/80 dark:bg-slate-800/80 border border-slate-200/60 dark:border-slate-700/60 rounded-full shadow-lg px-3 py-2 md:px-4 md:py-3 flex items-center justify-between"><Link href="/" className="flex items-center"><Image src="/logo-light.png" alt="Agentive logo" width={845} height={160} priority className="h-7 md:h-8 w-auto dark:hidden" /><Image src="/logo-dark.png" alt="Agentive logo" width={861} height={160} priority className="hidden h-7 md:h-8 w-auto dark:block" /></Link><div className="hidden md:flex items-center gap-6">{links.map(link => <Link key={link.href} href={link.href} className="text-sm text-slate-600 dark:text-slate-300 hover:text-brand-600">{link.label}</Link>)}</div><div className="hidden md:block"><WhatsAppButton text="Book a Free Demo" size="sm" /></div><button className="md:hidden p-2" onClick={() => setOpen(!open)} aria-label={open ? "Close menu" : "Open menu"}>{open ? <X size={20} /> : <Menu size={20} />}</button></div>{open && <div className="md:hidden mt-2 bg-white/95 dark:bg-slate-800/95 border dark:border-slate-700 rounded-2xl shadow-lg p-4 space-y-3">{links.map(link => <Link key={link.href} href={link.href} onClick={() => setOpen(false)} className="block text-sm py-2">{link.label}</Link>)}<WhatsAppButton text="Book a Free Demo" size="sm" className="w-full" /></div>}</nav>; }
