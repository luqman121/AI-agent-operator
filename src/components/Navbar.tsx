"use client";
import { Menu, X } from "lucide-react";
import Link from "next/link";
import { useState } from "react";
import WhatsAppButton from "./WhatsAppButton";
const links = [{ label: "الحل", href: "/#how-it-works" }, { label: "المميزات", href: "/#features" }, { label: "القطاعات", href: "/#industries" }, { label: "الأسئلة", href: "/#faq" }, { label: "جرّب مجانًا", href: "/demo" }];
export default function Navbar() { const [open, setOpen] = useState(false); return <nav className="fixed top-3 left-3 right-3 md:top-4 z-50 mx-auto max-w-4xl"><div className="backdrop-blur-xl bg-white/80 dark:bg-slate-800/80 border border-slate-200/60 dark:border-slate-700/60 rounded-full shadow-lg px-3 py-2 md:px-4 md:py-3 flex items-center justify-between"><Link href="/" className="font-bold text-lg"><span className="text-brand-500">Hermes</span>AI</Link><div className="hidden md:flex items-center gap-6">{links.map(link => <Link key={link.href} href={link.href} className="text-sm text-slate-600 dark:text-slate-300 hover:text-brand-600">{link.label}</Link>)}</div><div className="hidden md:block"><WhatsAppButton text="احجز عرضًا مجانيًا" size="sm" /></div><button className="md:hidden p-2" onClick={() => setOpen(!open)} aria-label={open ? "إغلاق القائمة" : "فتح القائمة"}>{open ? <X size={20} /> : <Menu size={20} />}</button></div>{open && <div className="md:hidden mt-2 bg-white/95 dark:bg-slate-800/95 border dark:border-slate-700 rounded-2xl shadow-lg p-4 space-y-3">{links.map(link => <Link key={link.href} href={link.href} onClick={() => setOpen(false)} className="block text-sm py-2">{link.label}</Link>)}<WhatsAppButton text="احجز عرضًا مجانيًا" size="sm" className="w-full" /></div>}</nav>; }
